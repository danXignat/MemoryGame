<UserControl x:Class="MemoryGame.Views.HomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MemoryGame.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="#ecf0f1">
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
            <TextBlock Text="Memory Game" FontSize="48" FontWeight="Bold" Foreground="#2c3e50" Margin="0,0,0,30"/>
            <TextBlock Text="Exercise your memory with this fun matching game!" FontSize="20" 
                       Foreground="#7f8c8d" TextAlignment="Center" Margin="20,0,20,50"/>

            <Button Command="{Binding StartGameCommand}" Content="Start Game" Width="200" Height="50" 
                    Background="#2ecc71" Foreground="White" FontSize="20" BorderThickness="0">
                <Button.Resources>
                    <Style TargetType="Border">
                        <Setter Property="CornerRadius" Value="25"/>
                    </Style>
                </Button.Resources>
            </Button>
        </StackPanel>
    </Grid>
</UserControl>